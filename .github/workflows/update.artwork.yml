name: Update Artwork Manifest and Deploy

on:
  push:
    branches: [ main ]
    paths:
      - 'pictures/artwork/**'

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    if: github.event.head_commit.message != 'Auto-update artwork manifest [skip ci]'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install chokidar

      - name: Generate artwork manifest
        run: node scripts/generate-artwork-manifest.js

      - name: Commit manifest
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/artwork-manifest.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update artwork manifest [skip ci]"
            git push

      - name: Build site
        run: |
          # Example for a simple static folder; replace with your build if needed
          mkdir -p _site
          cp -r index.html style/ scripts/ pictures/ _site/

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          path: _site

          